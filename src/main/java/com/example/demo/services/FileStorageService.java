package com.example.demo.services;

import org.springframework.stereotype.Service;

@Service
public class FileStorageService {
	public byte[] compressPdf(byte[] data) {
        // Implémentez la compression si nécessaire
        return data;
    }
    
    public byte[] decompressPdf(byte[] data) {
        // Implémentez la décompression si nécessaire
        return data;
    }

}
